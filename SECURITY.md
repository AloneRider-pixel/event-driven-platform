# Security Policy

Report suspected vulnerabilities privately through GitHub's security reporting mechanism when available. Include affected services, reproducible steps, and sanitized logs. Never publish credentials or tokens.

The platform is designed around authenticated APIs, idempotent event handling, rate limiting, retries, dead-letter queues, and isolated service boundaries. CI also runs automated dependency and CodeQL checks.
